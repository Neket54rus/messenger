import { FC } from 'react'
// import { useParams } from 'react-router-dom'

import styles from './DialogPage.module.scss'

export const DialogPage: FC = () => {
	// const { id } = useParams()

	return <div className={styles.DialogPage}>DialogPage</div>
}
